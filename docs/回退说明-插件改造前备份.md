# 插件系统改造 · 改动前备份与回退说明

改造开始时间：2026-09-22
基线状态：`python -B -m unittest discover -s tests` → **Ran 145 tests · OK**

> 注：`tests/` 与 `tools/check_*.py` 已在 v3.0 公开发布时从仓库移除。本文中的测试与检查命令描述的是改造当时的状态，现已不可执行，仅作历史记录。

本次改造（模块化插件系统）动工前建立了三层独立备份，任意一层都能把项目还原到改造前的原始状态。

---

## 第 1 层：git 仓库（推荐，支持逐行回退）

项目原本不是 git 仓库，本次已初始化并提交了原始状态。

| 项目 | 值 |
| --- | --- |
| 提交 | `5560a2c` |
| 提交信息 | `backup: 插件系统改造前的原始状态（基线：145 测试通过）` |
| 纳入版本控制的文件 | 119 个 |

常用回退命令（在项目根目录执行）：

```powershell
# 看改了什么
git status
git diff

# 只还原某一个文件（例如改坏了 main.py）
git checkout 5560a2c -- src/main.py

# 放弃全部改动，回到改造前
git reset --hard 5560a2c

# 想看某个文件改造前的原文
git show 5560a2c:src/main.py
```

> 说明：`dist/`、`release/`、`data/`（运行数据）、`__pycache__/` 依 `.gitignore` 不纳入版本控制，
> 因此 git 只管源码，不会碰到你的配置、记忆和已构建的 EXE。

---

## 第 2 层：原始文件完整副本（最直观，直接复制即可）

```
release\backup-20260922_195249\
├── src\            （含 pet_tools\ 全部源码）
├── tools\
├── tests\
├── docs\
├── resources\      （立绘、prompt.txt、skills\）
├── data\           （config.example.json / README.md）
├── README.md
├── requirements.txt
├── .gitignore
└── 启动桌宠.bat
```

共 122 个文件、31,974,799 字节。

回退方式：把需要的文件从该目录复制回项目根目录覆盖即可，例如

```powershell
Copy-Item -Recurse -Force "release\backup-20260922_195249\src\*" "src\"
```

（该目录不含 `dist\`、`release\`、构建缓存，是干净的源码快照。）

---

## 第 3 层：校验过的源码 ZIP

| 项目 | 值 |
| --- | --- |
| 路径 | `release\虹语织-桌宠v2-源码-20260922_195258.zip` |
| 文件数 | 119 |
| 大小 | 24,996,043 字节 |
| SHA-256 | `5c6701b03a904a43ed5357cac12efb5e793d0d61dfb29f798924aff2b9c8a6cd` |

该 ZIP 由项目自带的 `tools/package_source.py` 生成，生成时已逐文件校验 SHA-256，
并确认未混入 `config.json` / `memory.json` / `action_log.json` 等本机运行数据
（即不含 API Key、记忆与日志）。

解压后即为可运行的源码版桌宠。

---

## 另外：改造前的 EXE 已另存，可作为回退点

改造前 `dist\` 里那个 EXE 已在动工前另存到 `release\`，不会被覆盖：

- `release\虹语织-桌宠v2-旧版-20260922_preplugins.exe`（56,516,070 字节，**改造前的 dist 产物**）
- `release\虹语织-桌宠v2.exe`（2026-09-06 构建，55,361,453 字节）
- `release\虹语织-桌宠v2-20260915.exe.zip`（EXE 压缩归档）

> 注意：`dist\虹语织-桌宠v2.exe` 已被改造后的构建覆盖，不再是改造前的版本；
> 要跑改造前的 EXE 请用上面 `release\` 里的 `…preplugins.exe`。

---

## 回退优先级建议

1. 只是某个文件改错 → 用第 1 层 `git checkout 5560a2c -- <文件>`；
2. 想整体回到改造前 → 用第 1 层 `git reset --hard 5560a2c`，或用第 2 层整目录覆盖；
3. 源码目录整个丢了 → 用第 3 层 ZIP 解压还原。

---

## 改造后的提交记录

| 提交 | 说明 |
| --- | --- |
| `5560a2c` | **改造前的原始状态**（基线：145 测试通过）← 回退目标 |
| `7ed39b5` | `feat(plugins): 模块化插件系统 pet_plugins（源码版与 EXE 版通用）` |
| `b2921c6` | `docs: 验证记录校准为最终 EXE 产物与探针插件结果` |
| `f2a0181` | `fix(plugins): 控制中心插件开关把布尔值当插件名 + 总开关立即生效` |
| （后续提交） | `chore: 版本号提升到 3.0（UA / EXE 名 / 控制中心）` |

改造后的验收结果（详见 `docs/plugin-system-verification-2026-09-22.json`）：

- 单元/集成测试：181 个全部通过（原 145 + 新增 36）；
- GUI 真实界面自动冒烟：`python -B tools/check_plugins_gui.py` → 24/24 通过；
- EXE 冻结版端到端：真实加载插件成功，状态文件正确写入 exe 同级 `plugin_state/`，插件目录未被污染。

想回到改造前，除了上面的 git / 目录副本 / ZIP 三种方式，**旧 EXE 也原样保留**：

- `release\虹语织-桌宠v2-旧版-20260922_preplugins.exe`（56,516,070 字节，改造前的 dist 产物）
- `release\虹语织-桌宠v2.exe` 与 `release\虹语织-桌宠v2-20260915.exe.zip`（更早的归档）

当前构建（v3.0）在 `dist\虹语织-桌宠v3.exe`（56,625,282 字节）；
版本提升到 3.0 之前的插件版构建仍在 `dist\虹语织-桌宠v2.exe`（56,625,091 字节），
确认新版没问题后可以自行删除。

改造过程中的源码包快照：

| 文件 | 说明 |
| --- | --- |
| `release\虹语织-桌宠v2-源码-20260922_195258.zip` | 改造**前**的源码（sha256 `5c6701b0…`，119 文件） |
| `release\虹语织-桌宠v2-源码-20260922_210739.zip` | 改造**后**的源码（sha256 `2851d1dd…`，133 文件） |
