# 根目录整理记录

根目录仅保留 `README.md`、`requirements.txt`、`启动桌宠.bat` 和隐藏的 `.gitignore`。代码集中在 `src/`，立绘、人设与技能集中在 `resources/`，本机配置、记忆和日志集中在 `data/`。使用说明与完整功能介绍在 `docs/`，检查和打包脚本在 `tools/`。

`src/pet_paths.py` 统一解析路径，源码从任意工作目录启动均使用项目的资源和数据。旧根目录的三个运行数据文件可在首次启动时自动迁移；目标已存在时拒绝覆盖。EXE 保持同目录保存数据和技能的便携布局。重启按钮与根目录启动脚本均指向 `src/main.py`。

构建脚本 `tools/build_exe.py` 将构建缓存和日志放在 `tools/.build/`，EXE 输出到 `dist/`；源码包放在 `release/`，按新布局收录并排除真实运行数据和构建缓存。配置管理技能和运行时提示已同步到实际配置路径，也移除了技能中残留的旧搜索模型配置说明。

验证结果：

- 37 项回归检查通过，覆盖新旧数据路径、冲突保护、EXE 便携路径、重启入口、搜索和源码打包。
- 三个迁移的运行数据文件逐一校验 SHA-256，内容未变。
- 从临时工作目录进行隔离启动，十种情绪素材、分层动画、透明窗口及六个技能均加载正常；没有 Tk 回调异常。测试实例已自动退出。
- EXE 构建耗时 31.89 秒，检查确认包内含路径模块、搜索模块、解析依赖、立绘、人设和技能。产物位置及哈希见 `root-layout-verification-2026-09-06.json`。

迁移时工作区原 `pythonw main.py` 进程已经退出，当前运行的是 `D:\Nijikori Pet` 的独立 EXE。独立版本继续运行；本次启动验证使用隔离配置，不读取聊天密钥或执行定时任务。

原根目录代码与构建配置已移入同盘恢复目录，未删除；恢复路径与移动清单见 `root-layout-migration-2026-09-06.json`。个人数据、当前资源和历史发布均保留。
